package com.store.main.model.enums;

/**
 * Enum representing the type of discount a voucher provides.
 * - PERCENT: Discount is calculated as a percentage of the order total (e.g., 10%)
 * - FIXED: Discount is a fixed amount (e.g., $10 off)
 */
public enum VoucherType {
    PERCENT,
    FIXED
}
